# TEMPLATE: Docker Compose Configuration for New Service
# Copy this template and replace {placeholders} with your service information
# 
# CHECKLIST:
# [ ] Choose application port (8081-8089) - check SERVICE_PORT_ALLOCATION.md
# [ ] Choose PostgreSQL port (5433-5439) - check SERVICE_PORT_ALLOCATION.md
# [ ] Replace {service-name} with your service name (e.g., "user", "account")
# [ ] Replace {port} with chosen PostgreSQL port (e.g., "5434")
# [ ] Create init script at: docker/init-scripts/{service-name}-init.sql
# [ ] Update SERVICE_PORT_ALLOCATION.md with your port allocation
# [ ] DO NOT add Kafka/Zookeeper if they already exist in docker-compose.yml

version: '3.8'

services:
  # PostgreSQL Database for {Service Name} Service
  postgres-{service-name}:
    image: postgres:15-alpine
    container_name: postgres-{service-name}-service
    environment:
      POSTGRES_DB: {service-name}_db
      POSTGRES_USER: {service-name}_user
      POSTGRES_PASSWORD: {service-name}_password
    ports:
      - "{port}:5432"  # Replace {port} with allocated port (e.g., "5434:5432")
    volumes:
      - postgres-{service-name}-data:/var/lib/postgresql/data
      - ./docker/init-scripts/{service-name}-init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - banking-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U {service-name}_user -d {service-name}_db"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ONLY ADD IF Kafka/Zookeeper DON'T EXIST IN docker-compose.yml
  # Zookeeper for Kafka
  # zookeeper:
  #   image: confluentinc/cp-zookeeper:7.5.0
  #   container_name: zookeeper
  #   environment:
  #     ZOOKEEPER_CLIENT_PORT: 2181
  #     ZOOKEEPER_TICK_TIME: 2000
  #   ports:
  #     - "2181:2181"
  #   networks:
  #     - banking-network
  #   healthcheck:
  #     test: ["CMD", "nc", "-z", "localhost", "2181"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

  # Kafka Broker
  # kafka:
  #   image: confluentinc/cp-kafka:7.5.0
  #   container_name: kafka
  #   depends_on:
  #     zookeeper:
  #       condition: service_healthy
  #   ports:
  #     - "9092:9092"
  #     - "9093:9093"
  #   environment:
  #     KAFKA_BROKER_ID: 1
  #     KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
  #     KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://localhost:9092,PLAINTEXT_INTERNAL://kafka:29092
  #     KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_INTERNAL:PLAINTEXT
  #     KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT_INTERNAL
  #     KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
  #     KAFKA_AUTO_CREATE_TOPICS_ENABLE: "true"
  #   networks:
  #     - banking-network
  #   healthcheck:
  #     test: ["CMD", "kafka-broker-api-versions", "--bootstrap-server", "localhost:9092"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

volumes:
  postgres-{service-name}-data:

networks:
  banking-network:
    driver: bridge

